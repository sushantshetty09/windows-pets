# windows-pets

A persistent desktop pet system that boots with Windows, roams freely, and interfaces with the Antigravity chatbox abilities.

## Setup

1. Run `pip install -r requirements.txt` to install dependencies.
2. Run `python install_startup.py` to register the pet to boot with Windows.
3. Use `python autopush.py` to quickly sync changes back to GitHub.
